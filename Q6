#include <iostream>
using namespace std;
int main() {
    int l, n;
    std::cin>>l;
    std::cin>>n;
    while (n--) {
        int w, h;
        std::cin>>w>>h;

        if (w<l || h<l ) {
            printf("UPLOAD ANOTHER\n");
        } else if (w == h) {
            printf("ACCEPTED\n");
        } else {
            printf("CROP IT\n");
        }
    }
    return 0;
}
